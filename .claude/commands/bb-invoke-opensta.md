---
description: "OpenSTA static timing analysis wrapper — synth-stage or post-PD multi-corner signoff. Direct invocation of bb-invoke-opensta."
---

Run the `bb-invoke-opensta` skill with the user's arguments.

Expects `netlist`, `sdc_path`, `tech_lib`, `top_module`, `design_name`, optional `spef` / `mode` / `corners`. See `.claude/skills/bb-invoke-opensta/SKILL.md`.
