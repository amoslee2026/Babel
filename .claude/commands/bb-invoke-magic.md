---
description: "Magic layout tool wrapper — floorplan/place/drc/extract. Direct invocation of bb-invoke-magic."
---

Run the `bb-invoke-magic` skill with the user's arguments.

Expects `tech_file`, `layout_input`, `action`, `design_name`, `top_module`. See `.claude/skills/bb-invoke-magic/SKILL.md`.
